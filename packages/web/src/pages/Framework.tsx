import { Page } from './Page';

export function Framework() {
  return (
    <Page number="06" title="Framework">
      <p>
        The results framework — Goal → Outcome → Output → Activity — with indicators,
        baselines, and targets. Build it under Configure once indicators exist.
      </p>
    </Page>
  );
}
