import { Page } from './Page';

export function Maps() {
  return (
    <Page number="04" title="Maps">
      <p>
        Maps light up after org units with geometry exist: draw or import boundaries and
        facility points under Configure → Org units, then map indicator values against
        targets here.
      </p>
    </Page>
  );
}
