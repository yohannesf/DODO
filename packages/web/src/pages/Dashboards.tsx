import { Page } from './Page';

export function Dashboards() {
  return (
    <Page number="03" title="Dashboards">
      <p>
        No dashboards yet. Dashboards are assembled from indicators — define data elements
        and indicators under Configure, then compose KPI cards, charts, maps, and pivot
        tables here.
      </p>
    </Page>
  );
}
